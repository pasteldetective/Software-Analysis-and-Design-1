/*
Author: Jackie Yee
Course: CSCI-136
Instructor: 
Assignment: Lab 7B

program indents file
*/

#include <iostream>
using namespace std;

int countChar(string line, char c)
{
    //int increment = 0;
    //for(int i = 0; i < line.size(); i++)
    //{
    //    increment += line[i];
    //}
    //return increment;
}


string removeLeadingSpaces(string line)
{
    /* string nline;
    bool location = false;
    for (int i = 0; i < line.length(); i++)
    {
        if (!isspace(line[i]))
        {
            location = true;
        }
        if (location)
        {
            nline += nline[i];
        }
    }
    return nline; */
}

int main()
{
    /* string line, nline, tabs;
    int tab = 0;

    while(getline(cin,line, '\n'))
    {
        string nline = removeLeadingSpaces(line);
        for (int i = 0; i < (nline[0]); i++)
        {
            tabs += '\t';
        }
        cout << tabs << nline << endl;
        tab += countChar(line, '{') - countChar(line, '}');
        
    }
*/
}